# Bibliotecas necessárias para o sketch HoloPass (Edge Computing)
#
# Nenhuma biblioteca externa é necessária.
# O código usa apenas recursos nativos do core do Arduino:
#   - math.h         (sin, cos, asin, sqrt)
#   - radians()      (macro do Arduino)
#   - tone()/noTone()(geração de som no buzzer)
#
# Em um protótipo com GNSS real, seriam adicionadas:
#   - TinyGPS++      (parser de sentenças NMEA do módulo GPS)
#   - SoftwareSerial (comunicação com o módulo NEO-6M)
